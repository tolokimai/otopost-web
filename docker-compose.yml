services:
  backend:
    build: ./backend
    container_name: otopost-backend
    ports:
      - "8000:8000"
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.5-flash}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-http://localhost:8000}
      CLIP_SERVER_TOKEN: ${CLIP_SERVER_TOKEN:-}
      YTDLP_COOKIES: ${YTDLP_COOKIES:-}
      CORS_ORIGINS: ${CORS_ORIGINS:-*}
    volumes:
      - clips:/data/clips
    restart: unless-stopped

  # Frontend Next.js — opsional. Bisa juga dideploy ke Vercel.
  frontend:
    build: ./frontend
    container_name: otopost-frontend
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_BASE: ${NEXT_PUBLIC_API_BASE:-http://localhost:8000}
    depends_on:
      - backend
    restart: unless-stopped
    profiles: ["with-frontend"]

  # ===== Fase 2 (SaaS) — aktifkan saat siap =====
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: otopost
  #     POSTGRES_PASSWORD: otopost
  #     POSTGRES_DB: otopost
  #   volumes: [pgdata:/var/lib/postgresql/data]
  # redis:
  #   image: redis:7-alpine

volumes:
  clips:
  # pgdata:
